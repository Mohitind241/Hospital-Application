apiVersion: v1
kind: Service
metadata:
  name: {{ include "hospital-app-chart.fullname" . }}
  labels:
    {{- include "hospital-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hospital-app-chart.selectorLabels" . | nindent 4 }}
